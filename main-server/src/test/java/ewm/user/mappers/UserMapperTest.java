package ewm.user.mappers;

import ewm.user.dto.NewUserRequest;
import ewm.user.model.User;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

import static org.assertj.core.api.AssertionsForClassTypes.assertThat;


@SpringBootTest()
class UserMapperTest {
//    final UserMapper userMapper;
    @Test
    public void toUser() {
//        NewUserRequest newUserRequest = new NewUserRequest();
//        newUserRequest.setName("Name");
//        newUserRequest.setEmail("Email");
//        User user = mapper.toUser(newUserRequest);
//        assertThat(user).usingRecursiveComparison().ignoringExpectedNullFields().isEqualTo(newUserRequest);
    }
}
